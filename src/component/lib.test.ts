/// <reference types="vite/client" />

import { describe, expect, test } from "vitest";

describe("idempotent-workpool", () => {
  test("placeholder", async () => {
    expect(true).toBe(true);
  });
});
